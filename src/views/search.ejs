<!DOCTYPE html>
<html lang="zh-cn">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>搜索结果</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1>搜索结果</h1>
    <form action="/search" method="GET">
        <input type="text" name="query" placeholder="输入搜索关键字" required>
        <button type="submit">搜索</button>
    </form>
    <h2>找到内容:</h2>
    <ul>
        <% if (pdfs.length > 0) { %>
            <% pdfs.forEach(function(pdf) { %>
                <li>
                    <% const pdfName = pdf.replace('.pdf', ''); %>
                    <a href="/pdfs/<%= pdf %>" class="text-blue-500 hover:underline"><%= pdfName %></a>
                </li>
            <% }); %>
        <% } else { %>
            <li>未找到相关内容。</li>
        <% } %>
    </ul>
    <a href="/">返回主页</a>
</body>
</html>